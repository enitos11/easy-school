apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "easyschool-chat.fullname" . }}-test-connection"
  labels:
    {{- include "easyschool-chat.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "easyschool-chat.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
