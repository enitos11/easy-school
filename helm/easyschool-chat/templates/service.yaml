apiVersion: v1
kind: Service
metadata:
  name: {{ include "easyschool-chat.fullname" . }}
  labels:
    {{- include "easyschool-chat.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "easyschool-chat.selectorLabels" . | nindent 4 }}
